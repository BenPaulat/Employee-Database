// bring in inq, db, console.table
// function to load main prompts (inquirer)
// choices = [of objects] (name, value) .then (Switch statement = case (value from prev) function view departments (call sql query))

const Db = require('./db/index');
const consoleTable = require('console.table');
